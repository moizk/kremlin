<%= form_for [@project, @project_entry] do |f| %>
  <% if @project_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_entry.errors.count, "error") %> prohibited this project_entry from being saved:</h2>

      <ul>
      <% @project_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment, rows: 3 %>
  </div>
  <div class="field">
    <%= f.hidden_field :end, class: "timepicker", value: "#{Time.now}" %>
  </div>
  <div class="actions nudge-down">
    <%= f.submit "Submit", class: "btn btn-red" %>
  </div>
<% end %>
